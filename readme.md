# Fast-Api test repo

This repository is meant to experiment a bit with FastAPI for knowledge expadsture purpouses.


# HowTo
Run the project using uvicorn: `uvicorn main:api --host 0.0.0.0 --port 8000 --reload`
